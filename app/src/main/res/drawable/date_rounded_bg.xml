<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

  <stroke
      android:width="1dp"
      android:color="@color/teal300" />

  <padding
      android:left="@dimen/date_padding"
      android:right="@dimen/date_padding"
      android:bottom="@dimen/date_padding"
      android:top="@dimen/date_padding" />

  <corners android:radius="@dimen/rounded_radius" />

</shape>